<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-zinc-900 text-white">
    <div class="w-full flex flex-col min-h-screen p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
       <h5 class="mb-5">Edit your post.</h5>
       <form method="post" action="/update/<%= post._id %>">
        <textarea class="p-3 w-full resize-nome rounded-md bg-transparent border-2 border-zinc-800 outline-none" name="content"><%= post.content %></textarea>
        <input class="px-3 py-2 w-40 text-sm rounded-md bg-yellow-500 text-black mt-5 cursor-pointer" type="submit" value="Update Post">
       </form>

    </div>
</body>
</html>